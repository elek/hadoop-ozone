name: test-key-input-stream
on:
  schedule:
   - cron: '*/30 * * * *'
jobs:
  it-client:
    name: it-client
    runs-on: ubuntu-18.04
    steps:
        - uses: actions/checkout@master
          with:
            ref: master-test
        - uses: ./.github/buildenv
          with:
             args: ./hadoop-ozone/dev-support/checks/integration.sh -Dtest=TestKeyInputStream
        - uses: actions/upload-artifact@master
          if: always()
          with:
            name: it-client
            path: target/integration
